# Proyecto de practica: React y firebase. solo eso

## Para el backend.
 - Como el backend será manejado con Firebase es necesario instalar el manejador del mismo.
    ```
    npm install firebase
    npm i validator
    npm install sweetalert2
    ```
